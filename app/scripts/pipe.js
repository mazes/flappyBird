window.Pipe = (function() {
    'use strict';

    var Pipe = function(el, game) {
        //this.el = el;
        //this.game = game;
    };

    Pipe.prototype.createPipe = function() {

    };

    return Pipe;
})();
